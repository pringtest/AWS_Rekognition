{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.799.0",
        "q": "^1.5.1"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "scripts": {
        "testall": "jest",
        "test1": "jest -i indexFaces.js",
        "test2": "jest -i searchFaceByImage.js",
        "integ-test": "jest --roots __tests__/integration"
    }
}
